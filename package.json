{
  "name": "kit",
  "private": true,
  "scripts": {
    "build": "pnpm -r --parallel --filter=./packages/* run build"
  },
  "workspaces": [
    "docs",
    "packages/*"
  ],
  "devDependencies": {
    "svelte": "^4.2.2"
  }
}
